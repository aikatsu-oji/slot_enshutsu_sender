{
  "name": "slot_enshutsu_sender",
  "version": "1.0.0",
  "private": true,
  "description": "Twitch配信用 パチスロ演出オーバーレイ: WebSocket中継サーバー + 主制御/副制御シミュレータ + コンパネ + 筐体ビュー",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server/trigger_relay_server.js",
    "dev": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/dev.ps1",
    "dev:start": "npm run dev -- start",
    "dev:stop": "npm run dev -- stop",
    "dev:status": "npm run dev -- status",
    "dev:open": "npm run dev -- open",
    "test": "npm run dev -- test",
    "check": "node --check server/trigger_relay_server.js && node --check scripts/ws_send.js && node --check twitch/twitch_bridge.js && node --check twitch/eventsub.js && node --check twitch/auth.js && node --check twitch/chat_irc.js && node --check twitch/config.js && node -e \"JSON.parse(require('fs').readFileSync('twitch/rules.json'))\"",
    "twitch": "node twitch/twitch_bridge.js",
    "twitch:mock": "node twitch/twitch_bridge.js --mock twitch/mock_events.jsonl --speed 5"
  },
  "dependencies": {
    "ws": "^8.21.3"
  }
}
